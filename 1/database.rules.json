{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": true,
        ".write": true
      }
    },
    "questions": {
      ".read": true,
      "$questionId": {
        ".write": "!data.exists()",
        ".validate": "newData.hasChildren(['clue', 'answer', 'contributedBy', 'createdAt']) && newData.child('clue').isString() && newData.child('clue').val().length > 0 && newData.child('clue').val().length <= 200 && newData.child('answer').isString() && newData.child('answer').val().length > 0 && newData.child('answer').val().length <= 300 && newData.child('contributedBy').isString() && newData.child('createdAt').isNumber()"
      }
    }
  }
}
